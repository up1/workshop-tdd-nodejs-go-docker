# workshop-tdd-nodejs-go-docker

## Working with Docker

### Start database
```
$docker compose up -d postgres
$docker compose ps
$docker compose logs --follow
```

### Start API server with node
```
$docker compose up -d nodejs
$docker compose ps
$docker compose logs --follow
```
